{
  "name": "cube-tournament-host",
  "version": "0.5.0",
  "description": "Host (hub WebSocket + web app) per Host (hub WebSocket + web app) per il Cube Tournament Controller ESP32",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
